{% extends '/base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('acheteurAcheter') }}
{% endblock %}
{% block body %}
    <div class="group-acheter">
        <div>
            <h1>Ajouter une réalisation</h1>
            {{ form_start(form) }}
            <div class="row">
                <div class="col-4">
                    {{ form_row(form.nom) }}
                </div>
                <div class="col-4">
                    {{ form_row(form.adresse) }}
                </div>
                <div class="col-4">
                    {{ form_row(form.mail) }}
                </div>
                <div class="col-4">
                    {{ form_row(form.realisation) }}
                </div>
{#                {% if form.realisation %}#}
{#                    <div class="prix">#}
{#                        <h1>Prix : {{ form.realisation.getPrix() }}</h1>#}
{#                    </div>#}
{#                {% endif %}#}
            </div>
            <button class="btn btn-primary" type="submit">Acheter</button>

            {{ form_end(form) }}
        </div>

    </div>
{% endblock %}